# TestUnit
Non-insane automated testing library

# Proof of concept

```

An expression-of-bool where, if the expression evaluates false, the expression tree can be walked to generate a descriptive error message of what the comparison was that failed.

```
